🎁 Promo Landing (Webpack + SCSS)

Промо-лендинг с таймером обратного отсчёта и карточками предложений.
Собран на Webpack 5, с модульной SCSS-архитектурой и доступной (a11y) разметкой.

🚀 Быстрый старт
Установка зависимостей
npm install

Запуск dev-сервера
npm run start


Dev-сервер будет доступен по адресу:
👉 http://localhost:9000

Production-сборка
npm run build


Результат сборки появится в папке dist/.

📁 Структура проекта
src/
├── html/
│   ├── views/          # Страницы (точки входа)
│   └── includes/       # HTML-партиалы (header, footer, timer, cards)
│
├── scss/
│   ├── _tokens.scss    # Дизайн-токены (цвета, размеры, z-index)
│   ├── _globals.scss   # Reset / базовые стили
│   ├── _mixins.scss    # SCSS-миксины
│   ├── _fonts.scss     # Подключение шрифтов
│   ├── promo.scss     # Стили промо-блока
│   ├── cards.scss     # Стили карточек
│   ├── timer.scss     # Стили таймера
│   └── main.scss      # Главный SCSS-файл
│
├── js/
│   └── main.js         # Инициализация JS (Timer)
│
├── img/                # Изображения
├── fonts/              # Шрифты
└── favicon/            # Фавиконки

🧩 HTML-шаблоны

Используется HtmlWebpackPlugin + lodash templates.

Подключение партиалов
<%= _.template(require('./../includes/header.html'))(data) %>


📌 Все страницы лежат в src/html/views
📌 Общие блоки — в src/html/includes

🎨 SCSS-архитектура

Используется модульный SCSS + БЭМ.

Принципы

❌ без глобальных * {} кроме reset

✅ токены вынесены отдельно

✅ один компонент — один SCSS-файл

❌ без вложенности глубже 3 уровней

Подключение
@use 'tokens';
@use 'mixins';

.timer {
  // styles
}

⏱ Timer (JavaScript)

Таймер реализован как класс, автоматически инициализируется по атрибуту:

<div class="timer" data-timer data-duration="300"></div>

Возможности

⏱ Обратный отсчёт в секундах

👁 Пауза при скрытии вкладки

▶ Автовозобновление при возврате

♿ Accessibility (role="timer", <time datetime="">)

📡 События через CustomEvent

События
timer:start
timer:tick
timer:pause
timer:resume
timer:end
timer:stop


Пример подписки:

document.addEventListener('timer:end', () => {
  console.log('Таймер закончился');
});

🛠 Webpack
Основные технологии

Webpack 5

MiniCssExtractPlugin

CssMinimizerPlugin

TerserPlugin

CopyWebpackPlugin

Dev / Prod

development — sourcemaps, hot reload

production — минификация, очистка dist/